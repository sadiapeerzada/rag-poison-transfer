# Pinned dependency versions verified working on Kaggle (T4 GPU), captured 2026-09-01.
# Curated subset of `pip freeze` -- Kaggle's base image ships 700+ packages this repo
# never imports (spacy, cudf, bigquery, pymc, etc). Only what this repo actually
# depends on is listed, at versions confirmed working: config routing test passing,
# real HotpotQA loader run producing correct gold_doc_ids, full pytest suite (37 passed).
#
# Install: pip install -r requirements-kaggle-lock.txt
# For local Mac/MLX dev, no pinned lockfile exists yet -- use requirements.txt
# + `pip install mlx-lm` and pin one down once the Mac workflow is verified
# the same way this one was.

numpy==2.0.2
PyYAML==6.0.3
pytest==8.4.2
rank-bm25==0.2.2
sentence-transformers==5.4.1
datasets==5.0.0
torch==2.10.0+cu128
transformers==5.0.0
accelerate==1.13.0
bitsandbytes==0.50.2
