# UNPINNED local development environment (Mac/MLX dev).
# For reproducible/citable results on Kaggle/GPU, use requirements-kaggle-lock.txt instead.
# That file contains pinned versions verified working with the full test suite.
# This file is for fast iteration and debugging only, not for final experimental runs.

# Weeks 1-2 (smoke test) — CPU-only, no model downloads required
rank_bm25
pyyaml
pytest

# Weeks 3-4 (clean baselines) — install on your Mac:
sentence-transformers   # dense retrieval (BGE/E5) + cross-encoder reranker
datasets                # HotpotQA / 2WikiMultiHopQA / NQ-open loading (needs internet)
numpy

# Real generator, local Mac (already installed if you did exp_001):
# mlx-lm

# Real generator, Kaggle GPU (transformers + bitsandbytes 4-bit quantization) --
# needed for exp_004 onward (exp_004_hotpotqa_kaggle.yaml, exp_005/006/007):
# torch
# transformers
# accelerate
# bitsandbytes
# See requirements-kaggle-lock.txt for exact pinned versions verified working
# on Kaggle T4 GPU.
