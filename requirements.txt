# Weeks 1-2 (smoke test) — CPU-only, no model downloads required
rank_bm25
pyyaml
pytest

# Weeks 3-4 (clean baselines) — install on your Mac:
sentence-transformers   # dense retrieval (BGE/E5) + cross-encoder reranker
datasets                # HotpotQA / 2WikiMultiHopQA / NQ-open loading (needs internet)
numpy

# Real generator (already installed if you did exp_001):
# mlx-lm
